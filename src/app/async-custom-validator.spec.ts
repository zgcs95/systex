import { AsyncCustomValidator } from './async-custom-validator';

describe('AsyncCustomValidator', () => {
  it('should create an instance', () => {
    expect(new AsyncCustomValidator()).toBeTruthy();
  });
});
